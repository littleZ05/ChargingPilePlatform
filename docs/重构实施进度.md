# 重构实施进度

以工作区根目录《重构与待办计划_20260915.md》为范围，未完成项不因已有测试通过而取消。

## 已合并

- PR #30：独立构建、QtTest、JUnit与JSON验证入口。
- PR #31：会话身份、服务器订单ID、单活动订单、开单锁价、事务去重回执、资料持久化、自愈样本游标及事件原子性。提交 f9d3d3e 干净状态19个工程通过。
- PR #32：内嵌腾讯导航、驾车/步行选择、polyline修复；使用手册与测试方案初稿。实际WebEngine本地HTML加载测试通过，在线腾讯验收待执行。

## 当前预测分支

- 最近24样本选取修正，时域上限24小时，后台线程按值计算并丢弃过时界面结果。
- 按桩小时均值后按站汇总，重复记录不增加额外负荷，真实零值算作有效观测。
- 后台峰值80%容量预警，大屏输出24小时预测。
- 线程、数据与模型单测；合成时间留出评估比较OLS/WMA/最近值基线。合成误差不能宣称是真实站点精度。

## 后续仍需处理

1. 预测缺测插补与数据来源标记、按真实记录定价边界、低拥堵推荐、站内空闲桩估计、自愈阈值计算入口与误差说明。
2. 客户端恢复的桌面端到端验证，包括切站、账户切换、回执晚到和余额不足再次结算。服务层测试不能替代UI验收。
3. 后台 DatabaseManager 与 StationStore 仍有重复访问/建表逻辑；抽离路由、仓储和UI面板，落实单源文件长度规范。
4. 演示脚本不得默认重置业务数据；修复源码更新但二进制不重建的问题；可移植打包与迁移备份说明。
5. 更新需求矩阵、概要设计、交接文档、README、使用手册与测试方案最终版；整理实际测试结果和运行截图。
6. 按最终提交做全量回归、Qt桌面操作、三端数据对账和在线导航检查，自主审核并合并剩余PR。

P3独立桩端和生产级增强为原计划明确的后续扩展；当前不得宣称已经完成真实设备协议或生产认证。
