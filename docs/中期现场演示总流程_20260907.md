# 中期现场演示：全组演示内容与分步操作（2026-09-07）

> 本文件为**全组统一演示脚本**：谁演示、展示什么、每一步怎么操作、预期结果是什么。
> 代码基线：main（公开仓库）`4f05b9d`；每步前先 `git pull`。
> 配套：逐字稿《中期评审汇报_20260907.md》、演示操作备忘。

## 0 演示前准备（提前 20 分钟，各人自检）

```bash
cd ~/桌面/dongruan_ws/ChargingPilePlatform && git pull
# 三个程序分别构建（不要现场编译）
cd src/pcserver        && qmake6 && make            # 产物 PcServer
cd src/userclient      && qmake6 && make            # 产物 UserClient
cd src/innovation_demo && qmake6 && make            # 产物 InnovationDemo
```

- 窗口准备：需求矩阵 xlsx、概要设计 PDF(第2页)、GitHub 仓库页(PR/Branches)、
  PcServer、UserClient、InnovationDemo、浏览器大屏 index.html、sqlite3 终端、测试终端。
- 检查项：陈庚泉确认 PcServer 运行目录已生成 chargingpile.db 且含演示数据；
  葛伊诺确认联网（腾讯 key 可用）；吴羽桐确认大屏可断网刷新；毛悦琮确认本地进度代码就绪。

---

## 1 张芮萌（组长）演示

### 1.1 需求矩阵与项目总体（开场 0:00）
1. 打开 `01需求矩阵第4组张芮萌.xlsx`，滚到第 3~26 行；
2. 指向 D 列讲：23 项需求、▲ 必修 11 项；G 列讲：五人负责人；H 列讲：排期到 09-10；
3. 切浏览器 GitHub：仓库现为 **Public**；Pull requests 页显示 PR #1~#6 均已合并；
   点开任意 1-2 个 PR 停留 2 秒。
- 预期：矩阵与 PR 列表清晰可见。

### 1.2 概要设计结构图（0:40）
1. 打开 `docs/概要设计说明书第4组.pdf` 第 2 页，放大 100%；
2. 沿图讲：用户端 ←TCP帧→ 服务器端 ←SQL→ SQLite，Web 大屏只读。

### 1.3 创新点 1/2 模拟触发演示（6:00）
1. 启动 `./InnovationDemo`（运行目录自动建 innovation_demo.db）；
2. **动态计费**：选“城东科技园站” → “预测空闲率”设 **80%** → 点【应用价格策略】；
   - 预期：日志出现“【生效】…自动下调为 8 折”，策略标签变“闲时特惠 8 折”；
   - 再把空闲率设 **30%** → 应用 → 预期：日志“【关闭】恢复基础价”；
3. **自愈告警**：选“S1-P01” → 连点 3 次【模拟异常功率(低于阈值)】；
   - 预期：第 1、2 次显示“第 x/3 次，接近告警”，第 3 次显示“【自愈】…自动触发模拟远程重启”；
   - 点【恢复正常功率】→ 预期：日志“计数清零，状态 Normal”；
4. 说明：当前为手动输入模拟 ML 输出，答辩前由吴羽桐预测接口替换。

### 1.4 测试佐证（7:30）
```bash
cd src/innovation_demo/tests && ./innovation_tests   # 预期 Totals: 4 passed
# 口头：main 共 41 项测试通过（error_utils 5 + pcserver 32 + innovation 4）
```

### 1.5 风险与下一步收尾（8:00）
1. GitHub → Code → Branches：显示所有 feat 分支与 main 一致/领先情况；
2. 讲到毛悦琮时如实展示其分支状态；讲到腾讯 key 时说明已建 Issue #7（公开后需重置 key）；
3. 计划：09-08 毛悦琮销售业绩/吴羽桐 ML 接口 → 09-09 集成联调 → 09-10 答辩。

---

## 2 陈庚泉演示：数据库与充电站管理（1:00）

1. 启动 `./PcServer`；日志应出现“建库完成/演示数据就绪”；
2. sqlite3 终端：
   ```bash
   sqlite3 src/pcserver/chargingpile.db ".tables"
   # 预期：admins users stations piles orders marketing_strategy
   #      pile_health_metrics pile_power_logs（8 张表）
   ```
3. PcServer 界面：
   - 电站列表：应显示“城东科技园站/城南枢纽站…”含桩数与在线率；
   - 单击电站行 → 弹出站内电桩明细，**停留 3 秒**让在线率/状态刷新一次；
   - 点【新增电站】→ 填站名/地址/经纬度/桩数 → 确定 → 列表出现新电站且桩数一致。
- 预期：全部操作无崩溃、数据写入 chargingpile.db。

---

## 3 葛伊诺演示：充电用户端（2:40）

1. 启动 `./UserClient`（main 版本，PR#5 已合入）；
2. **电站列表页**：进入后应基于 IP 定位显示附近充电站（腾讯地点搜索 5km POI），
   展示名称/地址/距离；价格与空闲数当前为稳定哈希模拟值（如实说明，09-09 联调后接真实数据）；
3. **详情页**：点某电站 → 站内电桩信息；
4. **重新定位**：输入其他地址 → 先地址解析，失败自动回退地点搜索兜底（可演示断网时的降级提示）；
5. **个人资料页/地图页**：切换演示界面。
- 预期：联网时真实 POI 出现；断网时页面给出提示不崩溃。

---

## 4 吴羽桐演示：Web 大屏（4:10）

1. 浏览器打开 `src/webdashboard/index.html`（file:// 或本地服务均可）；
2. 展示：6 张 KPI 卡（设备规模/实时健康率/异常待检修/今日营收/实时总负荷/
   未来 6h 预测峰值）+ 桩状态环形图 + 近 24h 滚动负荷与预测面积图 +
   近 7 日营收/订单双轴图；
3. 大屏数据由 PcServer 的本地只读接口
   `http://127.0.0.1:8890/api/dashboard/overview` 每 10s 注入：
   桩状态/在线率来自 piles 实时库（与 PcServer 3s 状态模拟同源），
   营收订单来自 orders 已完成订单，24h 曲线真实聚合优先 + 仿真兜底；
4. 关闭 PcServer 或断网按 `Ctrl + F5` 强刷，验证自动回退离线演示数据
   （ECharts 来自本地 vendor，页面不崩），页脚会标注数据源；
5. 拖动窗口大小验证自适应。
- 预期：echarts 正常渲染（3 个 canvas）；PcServer 运行时可看到数字
  10s 内自动刷新，桩状态环形图与“电桩状态/异常”页联动。

---

## 5 毛悦琮演示：PC 服务器端基础管理（5:00）

> 现状：分支已同步 main；登录/销售业绩/电桩状态/桩管理**代码尚未推送到仓库**。
> 若答辩前已推送并合入，则按 5.1 演示；否则按 5.2 口径演示。

### 5.1（若已合入）操作
1. PcServer 登录页：输入 admin/123456 → 进入后台；
2. 电桩状态页：表格显示在用/闲置/故障数量与占比；
3. 销售业绩页：QChart 近 7/30 日折线与今日/本月/总营收；
4. 充电桩管理：列表 + 选中“远程重启”触发模拟指令。

### 5.2（未合入时）现场口径
1. 启动 main 的 PcServer，展示“服务器端工程可编译运行、数据库正常读取”；
2. 展示其复用的充电站管理界面与数据访问层（StationStore）；
3. 如实说明：NO.8-11 开发中，代码今日/明日提交 PR，09-08 收口合入 main。

---

## 6 全员联动演示（可选，时间够时）

- 服务器端新增电站 → sqlite3 查询确认 → 用户端/大屏数据后续联调（09-09 完成）；
- 现场跑 pcserver 测试：`cd src/pcserver/tests && ./tst_schema`（11 passed）。

## 7 兜底预案

| 故障 | 处理 |
|---|---|
| UserClient 地图无网/key 失效 | 页面有降级提示，讲“离线降级设计”；切详情页/个人页继续 |
| PcServer/UserClient 起不来 | 讲代码与测试，切测试终端跑 tst_schema |
| InnovationDemo 建库失败 | 删除运行目录 innovation_demo.db 重启，或改用 `:memory:` 说明 |
| 毛悦琮无本地实现 | 用 5.2 口径，不夸大 |
| 时间不足 | 跳过 1.4 测试与 6 联动，口头说明 41 项测试已通过 |

## 8 会后跟进（当天）

- 毛悦琮提交 PR（组长 review 后合入 main）；
- 葛伊诺完成充电流程/联调；吴羽桐完成 ML 预测接口；
- 腾讯 key 重置（Issue #7）；仓库保持公开状态由组长确认。
