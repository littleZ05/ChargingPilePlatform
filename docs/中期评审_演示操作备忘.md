# 中期评审 演示操作备忘（一页）

> 配合《中期评审汇报_20260907.md》使用。原则：提前开好窗口、念到（展示：…）只切屏不操作。
> 切屏建议由组长张芮萌统一负责，发言人只讲。

## 0 开场前 10 分钟准备

- [ ] 确认投影/大屏已接好，虚拟机桌面干净
- [ ] 仓库已 `git pull` 到最新 main（`8d72f9b`）
- [ ] 以下窗口全部开好并最小化，按序号切换：

## 1 待开窗口清单（按演示顺序）

| # | 窗口 | 打开方式/路径 | 演示时操作 |
|---|---|---|---|
| 1 | 需求矩阵 Excel | 共享/工作区 `01需求矩阵第4组张芮萌.xlsx` | 滚到 ▲ 行、负责人、预计日期列 |
| 2 | GitHub 仓库页 | 浏览器打开 PR 列表页 | 展示 PR#1/2/3 已合并、feat/geyinuo 待提 |
| 3 | 概要设计 PDF | docs/概要设计说明书第4组.pdf 第 2 页 | 展示系统结构图 |
| 4 | schema.sql | 编辑器打开 src/database/schema.sql | 略讲八张表 |
| 5 | sqlite3 终端 | `sqlite3 库文件` → `.tables` | 展示八张表名 |
| 6 | 编译终端 | 仓库根：`cd src/pcserver && qmake6 && make` | 现场演示编译通过（预留 1 分钟） |
| 7 | PcServer 运行窗口 | `./PcServer` | 列表→点站→看 3 秒刷新→新增电站 |
| 8 | UserClient 运行窗口 | `./UserClient`（葛伊诺本地分支） | 切 电站列表/详情/个人页/地图 |
| 9 | 大屏 HTML | 浏览器打开 `src/webdashboard/index.html` | 展示折线渲染；可断网 F5 验证离线 |
| 10 | 测试终端 | 见下“测试窗口” | 跑 1-2 个工程显示 passed |
| 11 | 用户端代码/分支 | `git log feat/geyinuo` 或 IDE | 讲“今天提评审”时展示 |

## 2 测试窗口（演示前预先构建好，现场只运行）

```bash
cd src/common/tests && qmake6 && make && ./error_utils_tests        # 5 passed
cd src/pcserver/tests
qmake6 schema_tests.pro && make && ./tst_schema                     # 11 passed
qmake6 stationstore_tests.pro && make && ./tst_stationstore         # 11 passed
qmake6 stationui_tests.pro && make && ./tst_stationui              # 10 passed
```

> 现场建议只跑 `tst_schema` 或 `tst_stationstore` 一个即可，其余口头说明共 37 项通过。

## 3 演示流程（约 5 分钟）

| 时间 | 内容 | 切到窗口 |
|---|---|---|
| 0:00 | 组长开场+总体进度 | #1 需求矩阵 → #2 GitHub PR |
| 0:45 | 概要设计/文档 | #3 概要设计 PDF |
| 1:00 | 陈庚泉：数据库 | #4 schema.sql → #5 .tables |
| 1:40 | 陈庚泉：充电站管理 | #7 PcServer（列表/明细/新增） |
| 2:30 | 葛伊诺：用户端 | #8 UserClient + #11 分支提交 |
| 3:10 | 吴羽桐：大屏 | #9 index.html（可断网 F5） |
| 3:30 | 毛悦琮：服务器端/编译 | #6 编译 → #7 已运行 |
| 4:00 | 组长：风险与下一步 | #2 分支/PR 状态 |
| 4:30 | 测试佐证（可选） | #10 测试终端 |
| 5:00 | 收尾 | 切回主页 |

## 4 兜底预案

- 程序起不来：先切 #4/#5 讲数据库与代码，说“运行环境正在恢复，稍后补演示”；
- 没网/地图打不开：直接讲离线降级设计，切 #9 大屏（本就离线）；
- UserClient 未合 main 被追问：如实说明“今天提评审、明天合入”，切 #11 展示分支进度；
- 时间不够：跳过 #10 测试和 #6 现场编译，改为口头说明（37 项测试已通过）。

## 5 各自提前 1 小时检查

- 陈庚泉：PcServer 编译+演示数据（3 个电站+若干桩）就绪；
- 葛伊诺：UserClient 页面可切换、地图降级页正常；
- 吴羽桐：大屏本地可开、断网可刷；
- 毛悦琮：编译终端干净可复跑；
- 张芮萌：窗口顺序贴在手边，全程控场。
