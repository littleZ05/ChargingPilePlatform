# 边界声明：本项目做了什么、没做什么

答辩与提交材料中必须与本文件保持一致。原则是**只声明真实运行过的内容**，未部署的组件给出等价实现与迁移路径，不用"已构建大数据平台"这类超出证据的表述。

## 一、真实运行的组件

| 组件 | 证据 |
|---|---|
| Python 3.10 标准库（无第三方依赖） | `stage2/*/**.py`，全部可 `python3` 直接运行 |
| SQLite（业务库读取 + 数仓落库 + 即席查询） | `build-demo/chargingpile.db`、`warehouse.db`（44 张表） |
| HTTP 服务与 RESTful API | `stage2/dashboard/server.py`，`/api/health` 等 6 个接口 |
| Vue3 + ECharts 离线大屏 | `stage2/dashboard/web/`（源码）与 `web/dist/`（构建产物，ECharts 按需打包，无 CDN、无外网依赖） |
| 自研算法实现 | K-Means（k-means++）、最小二乘回归、Pearson、IQR/3σ、季节基线、流式滚动窗口 |
| 自动化测试 | 清洗 7 项 + 采集 6 项 + 分析 7 项 + 预测 6 项 + 大屏 9 项（其中 2 项 HTTP 用例需能访问 127.0.0.1） |

## 二、未部署的组件及等价实现

本机实测：2 核 CPU、7.9 GB 内存；无 JDK、无 Hadoop/Spark/Flink/Kafka/Hive、无 Docker、无 MySQL；外网受限且无 root 权限，无法安装。

| 未部署 | 等价实现 | 迁移路径 |
|---|---|---|
| HDFS | 本地分层目录（`ods/dwd/dws/ads`）+ SHA256 清单 | 课程虚拟机 HDFS 就绪后，`hdfs dfs -put` 上传同构目录即可 |
| Hive / ClickHouse | SQLite 数仓 + SQL 脚本 | 表结构（见 `数据字典.md`）可直接翻译为 Hive DDL |
| Spark / Flink | 等价的 Python 实现（清洗规则、窗口聚合、MLlib 算法） | `demo1–demo4.py` 已给出 Spark 版课堂示例，规则与字段定义一致 |
| Kafka | 基于事件时间回放与滚动窗口的流式模拟 | 迁移时把回放源换成 Kafka topic 消费者 |
| Sqoop / Flume | 数据库全表抽取 + 日志结构化脚本 | 迁移时对应为 Sqoop job 与 Flume agent 配置 |
| YARN / K8s / Airflow | `run_all.sh` 顺序编排 | 迁移时改造成 Airflow DAG |
| Ranger / Kerberos | 只读连接 + user_id 哈希 + 公开只出聚合 | 迁移时接 LDAP/Ranger 策略 |

课程资料（共享目录 `大数据开发/大数据阶段环境`）自带 JDK 8u261 与 Hadoop 3.2.1 单机安装包，以及
Spark 3.4.1 虚拟机镜像；如果组员在自己电脑上完成伪分布式部署，可以补充**真实的 `jps` 与 `hdfs dfs -ls` 截图**
作为附加证据，但没有截图就不声明已部署。

## 三、数据使用边界（不可越界表述）

1. `chargingFees` 原字段合计 401.52，**币种与语义未核实**，不得称为营收、收入或利润。
2. 会话年份原始值为 `0014`/`0015`，**不得改写为 2014/2015**，也不做真实日/月/年趋势；小时与星期来自源字段校验通过的行。
3. `session_count` 是**会话次数**，不是充电桩数量，也不能等同成功支付订单数。
4. 零电量（55 条）与零费用（3016 条）**保留并标记**，不认定失败、免费或缺失。
5. 峰谷时段划分与分时电价（高峰 1.5 / 平时 1.0 / 低谷 0.7 元/kWh）是**项目设定的取值假设**，
   由此派生的 `estimated_fee` **不是真实收费、不是营收**，必须与原费用字段分列呈现、不相加。
   老师原始材料（说明会 PPT、清洗讲义、交付清单）中检索不到任何电价数字，展示层**不得写成老师原始材料中的数字**。
6. 桩类型中文标签（直流 / 交流 / 直交流一体）是**假设映射**；数据集里的编码 4 无对应标签，
   保留原编码并标注「待核编码4」，不并入任何一类、不猜语义。
7. 电池表 `dsv13r2` 与会话表 `nvv2t` 的 ID 数值重合**不构成业务关联**，不做 JOIN；电池时间精度不可恢复，不做时间序列。
8. 课程数据集为郑州站点，**与第一阶段 Qt 演示站（北京）不是同一批站点**，两组数据不做合并统计。
9. 采集到的平台侧数据（订单 128 条、设备功率 42 条、告警 1 条）是**课程演示库**，其规模不代表真实生产量级。

## 四、模型边界

- 预测按"星期 × 小时"建模，共 168 个单元；因年份不可信，不做真实日历预测。
- 留一星期交叉验证下季节基线 MAE ≈ 12.65 次/小时，相对误差较高；该结果**如实呈现**，不宣称高精度，也不用于产能承诺。
- 聚类结果用于描述站点画像，不代表经营优先级或因果结论。

## 五、不在本次范围

- 深度学习（LSTM/CNN）、XGBoost、Prophet 等重依赖模型。
- 真实第三方 API 采集（无外网时仅保留适配器与失败重试逻辑）。
- 第一阶段已按组长指示暂缓的在线导航模块。
- 生产级权限体系、集群监控与云上部署。
