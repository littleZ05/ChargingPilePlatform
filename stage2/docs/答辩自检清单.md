# 答辩前自检清单（第二阶段）

答辩当天 8:50 开始，建议 8:00 到场后按此顺序做一遍。

## 一、环境与启动（3 分钟）

```bash
cd /home/bit/桌面/dongruan_ws/ChargingPilePlatform

# 1) 数据链路是否完好（秒级返回，不重跑清洗）
ls /home/bit/vmware_share/大数据开发/06.清洗结果_20260916_v2/{dwd,ads,manifest.json}

# 2) 起大屏服务（带预测模型）
python3 stage2/dashboard/server.py \
  --data /home/bit/vmware_share/大数据开发/06.清洗结果_20260916_v2 \
  --model build-stage2/run-defense/model.json \
  --model-v2 build-stage2/run-defense/model_v2.json

# 3) 自检接口（9 个只读接口，缺 --model-v2 时预测类接口会返回 400）
curl -s http://127.0.0.1:8765/api/health
curl -s 'http://127.0.0.1:8765/api/overview?platform=ios' | head -c 200
curl -s 'http://127.0.0.1:8765/api/forecast?base=sessions&horizon=24' | head -c 200
curl -s 'http://127.0.0.1:8765/api/session-quantiles?target=duration_hours&facility=交流&period=normal&hour=9' | head -c 200
curl -s 'http://127.0.0.1:8765/api/stations' | head -c 200
curl -s -o /dev/null -w '%{http_code}\n' 'http://127.0.0.1:8765/api/report?kind=start_hour'
```

> 容器/代理环境下若接口无响应，先排除 `LD_PRELOAD` 劫持：用 `env -u LD_PRELOAD curl ...` 再试一次。
> 单测同理：`env -u LD_PRELOAD python3 -m unittest discover -s stage2/dashboard/tests`，
> 否则两个 HTTP 用例会被判定为 loopback 不可用而跳过。

## 二、演示动线（5 分钟）

1. 打开 `http://127.0.0.1:8765` → 展示 KPI（3395 会话、19723.69 kWh、105 站点、55 零电量）。
2. 切筛选：平台 = ios → 电量 = 正电量 → 观察 KPI 与所有图表联动。
3. 点"开始小时分布"的 11 点柱条 → 弹出该小时明细。
4. 切"电池样本" → 强调独立数据、不与会话关联。
5. 切"数据质量" → 讲清标签可重叠、不能相加成异常率。
6. 切"预测"视图 → 目标/跨度切换、分位数取值、站点画像表。
7. 终端演示 `python3 stage2/analysis/query.py --db warehouse.db --sql "..."` 与 `/api/forecast`。

## 三、兜底方案（服务起不来时）

- 截图：`stage2/dashboard/验收截图/01-运营概览.png`、`02-电池样本.png`（浏览器渲染实拍）。
- 离线汇报页：`大数据开发/07.清洗汇报材料/数据清洗可视化.html`（双击即可打开，无需服务）。
- 报告：`分析报告.md`、`预测评估_v2.md`、`质量报告.md` 可直接投屏讲结论。

## 四、必须说准的数据事实（答错会扣分）

1. 原字段费用 401.52：币种与语义未核实，**不是营收**。
2. 年份 0014/0015：不改写、不做真实日期趋势；小时/星期来自已校验字段。
3. 会话数 ≠ 桩数 ≠ 成功支付订单数。
4. 零电量 55 条、零费用 3016 条：保留并标记，不认定失败或免费。
5. 电池表与会话表**不做 JOIN**（ID 数值重合不构成业务关联）。
6. 未部署 Hadoop/Spark/Kafka/Hive：给的是等价实现 + 迁移路径。
7. 预测只有未来 24 小时的会话数与电量由岭回归上线；1 小时、6 小时跨度上线的是基线，别讲成"模型全面胜出"。
8. 站点画像 k=2、轮廓系数 0.6414，只有 56/105 个站点参与聚类，稀疏站点只标注不归类。

## 五、提交物核对（9/17 24:00 前）

| 提交物 | 位置 | 状态 |
|---|---|---|
| 小组代码-第4组 | `build-stage2-delivery/e3f0e053/小组代码-第4组-第二阶段源码.tar.gz` | 已生成 |
| 数据集-第4组 | 同目录 `数据集-第4组.tar.gz` | 已生成 |
| 测试用例-第4组 | 同目录 `测试用例-第4组.tar.gz`（旧版 29 项；当前为 163 项单测 + 大屏验收） | 需重新打包 |
| 分析预测产物 | 同目录 `分析预测产物-第4组.tar.gz` | 已生成 |
| 小组录屏 | 按 `docs/录屏脚本.md` 录制 → `小组录屏-第4组-第二阶段.mp4` | 待录 |
| 团队贡献度 | `docs/团队贡献度-第4组.md` / `.csv` → 填入课堂派模板 | 待确认比例 |
| 项目路演 PPT | 按 `docs/答辩PPT增补页.md` 在第一阶段 PPT 上追加 8 页 | 待制作 |
| 个人报告（9/18） | 实训总结、环评报告 | 各人自写 |
