# 第二阶段数据清洗

汇报制作请看 [PPT同学制作指导](PPT同学制作指导.md)。共享目录 `大数据开发/07.清洗汇报材料/` 已提供离线交互页面和10张可插入PPT的高分辨率PNG。

可视化可从已验证的清洗结果重新生成（先校验manifest中的全部哈希）：

```bash
python3 stage2/cleaning/build_presentation.py --input 清洗结果目录 --output 数据清洗可视化.html
```

生成页面内嵌本地ECharts及聚合数据，直接浏览器打开，不需要服务器或网络。支持切换分析维度和指标、下载PNG。仅含聚合/审计统计，不包含用户标识。它是清洗汇报辅助材料，不代替第二阶段业务大屏。

依据《数据清洗基本流程-完整版.docx》六步流程。第二阶段必做是多维分析与Web大屏，预测选做；本清洗为大屏提供有明确字段定义的明细与聚合，不修改第一阶段Qt项目。

## 运行

```bash
python3 stage2/cleaning/clean.py \
  --input /home/bit/vmware_share/大数据开发/04.数据集最终版 \
  --output /home/bit/vmware_share/大数据开发/06.清洗结果_20260916_v2
python3 -m unittest discover -s stage2/cleaning/tests -v
# 可选，安装openpyxl后导出Excel查看版：
python3 stage2/cleaning/export_xlsx.py 输出目录
```

输出目录必须不存在，重复运行应换新目录，不覆盖旧证据。清洗仅需Python标准库，Excel查看版另依赖openpyxl。当前数千行数据无需分布式运行；课程示例为Spark，可在后续需要部署Spark时复用本规则和对账基线，不能宣称本脚本已运行在Hadoop上。

## 六步和产物

1. 探查：全量字段类型、范围、缺失、不同值数量写入audit/profile_before.json。
2. 质量评估：完整性、唯一性、有效性、一致性、准确性、时效性逐项说明，不给未经依据的“100分”。
3. 制定规则：见下表；不盲目删异常/填均值，保留未知含义。
4. 执行：ODS原样复制，DWD标准化明细，错误隔离到quarantine，逐行动作到audit。
5. 校验：输入=保留+隔离；五维聚合行数/金额/电量对账；原始哈希不变；同输入重复运行输出可复现。
6. 报告：质量报告.md、manifest.json（输入/输出SHA256）、规则数量、可选Excel。

## 规则清单

| 规则 | 处理 | 原因 |
|---|---|---|
| R01 格式 | NFKC、首尾空白及BOM/零宽空格规范化；ODS不动 | 消除格式噪声，保留追溯 |
| R02 去重 | 全行重复仅保留一份；站点/会话主键冲突全部隔离 | 不随意选一个冲突版本；电池esd身份未核实不按其单列去重 |
| R03 站点 | 设备数正整数、必要标识/描述、更新日期格式校验 | 无效维度不用于关联；地理真实性未核实 |
| R04 会话 | 有限非负电量/费用、正时长、起止先后、站点关联校验；内部不一致标记 | 非法关键数值隔离，其他未知问题保留 |
| R04 年份 | 0014/0015保留raw；calendar_date为空，禁止真实日期趋势 | 加2000年星期也一致，不能据此证明年份；时区也未知 |
| R04 零值 | 55条零电量、3016条零费用保留并标记 | 不知道是否免费、失败或缺失；另按项目设定的分时电价派生`estimated_fee`，但不与原费用相加、不称收入 |
| R04 枚举 | 设施按课程资料注释的码表标为直流(1)/交流(2)/直交流一体(3)；编码4保留原值并标「待核编码4」；星期one-hot核对 | 该码表为项目采用的假设映射，老师原始材料无此表，因此不合并编码、不猜语义 |
| R04 时段 | 按开始小时划分高峰(8–11,18–21)/平时(12–17)/低谷(0–7,22–23)，写入`time_period` | 取值来源与字段说明见 `stage2/docs/数据来源与字段定义.md`；小时校验未通过时留空，不按低谷兜底 |
| R04 电价 | `estimated_fee = 电量 × 时段单价`（峰1.5/平1.0/谷0.7元/kWh），写`unit_price`与`estimated_fee` | 单价为项目设定的参考值，与原费用字段分列，不称真实收费或营收 |
| R05 电池 | 正负电流保留；绝对值超过40A只标记；SOC/电压/温度大小校验 | 电流符号可能代表充电方向；课堂阈值不是已证明错误界限 |
| R05 时间 | raw保留，timestamp空，time_series_usable=0 | 2.02E+13不能还原采样间隔；不凭行号造时序 |
| R05 单位 | available_energy保持来源单位待核；瞬时功率绝对值=V×abs(A)/1000 | 不把功率当能量，不从未知间隔积分 |

## 大屏字段定义

规则版本 `2.0`。字段与取值的唯一真源是代码 `stage2/common/contract.py` 与文档 `stage2/docs/数据来源与字段定义.md`，
本 README 与之一致；改字段或取值必须三处同改并升 `rule_version`。

- `source_row`是原CSV的行号（含表头，因此从2开始）。ID一直是字符串。
- `session_count`是会话次数，包含零电量；`positive_energy_count`是正电量会话次数，不是成功支付订单数。
- `total_kwh`是原始合法电量之和；`source_fee_total_unverified`是原字段费用求和，币种/语义未核实，禁止标“真实营收”。Decimal计算不引入float长尾。
- `time_period`取`peak`/`normal`/`off_peak`（英文唯一书写，中文只在标签层）；`unit_price`随段取值，`estimated_fee`为派生列。
- `estimated_fee`是按项目设定的分时电价算出的**参考估算值**（峰1.5/平1.0/谷0.7元×kWh），与原费用字段分列呈现、不相加，不称营收；
  `ads/*.csv` 里对应列名为 `estimated_fee_total`，manifest 里为 `fee_total_estimated_model`。
- `facility_label`由课程资料注释的码表得到，编码4写成`待核编码4`；会话与站点表都带 `facility_dictionary_assumed` 标记（旧版本是 `facility_dictionary_unverified`）。
- `duration_hours_total`为时长累计；超过24小时保留flag，不静默裁剪。
- 小时/星期聚合仅包括对应校验通过记录；维度有效性筛选不同，今后有坏数据时不要要求每个图总数无条件相同。
- `calendar_date`空的会话不做真实日/月趋势；可做来源小时/星期分布，说明它们不构成已证实的现代日期。
- battery不与sessions关联：esd和sessionId重合只能提示待核，不能证明是同一实体。
- 输出仍含用户标识，内部分析保留；公开展示只使用聚合结果，不展示原user_id。

## 当前全量结果

105站点、3395会话、1594电池记录全部保留；0条隔离。不删除数据并非没清洗：字段规范化、数值校验、关联验证、风险标记、时间可用性限制和分层聚合均已执行。

总电量19723.69kWh；原费用合计401.52（非已确认币种的收入）；估算电费合计23759.01元（按项目设定电价估算的派生值，非营收）。
峰谷样本为高峰1432/平时1938/低谷25次。具体异常数量及长时会话数量以本次生成的质量报告为准。根目录课堂part输出不作为重复数据源混入。

七项测试覆盖零值/负电流/ID保留、重复与冲突隔离、负数/SOC越界、外键缺失、Decimal聚合、可复现及拒绝覆盖，
以及时段边界（0/7/8/11/12/17/18/21/22/23点）与桩类型码表映射。实际数据另需原始SHA256和五维聚合对账。
