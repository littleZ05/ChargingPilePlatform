# 第4组 开发流程（每人必读）

> 配合 [分工与进度记录.md](分工与进度记录.md) 与 [src/README.md](src/README.md) 使用。

## 0. 三条铁律

1. **main 永远可编译可运行**：任何人不得直接向 main 提交开发代码，开发一律走自己的 `feat/` 分支，完成后 Pull Request 合入；
2. **公共契约先评审**：`src/database/schema.sql`（陈庚泉维护）与 `src/common/common.h`（公共通信/常量）是全组地基，改动前先在群里说明，评审通过后由维护人合入 main，其他人不得私改；
3. **禁止提交构建产物**：Makefile、CMake 缓存、`*.o`、`moc_*`、`ui_*.h`、可执行文件一律不进 Git（.gitignore 已处理，提交前用 `git status` 检查）。

## 1. 首次准备（只做一次）

```bash
# ① 克隆仓库
git clone git@github.com:littleZ05/ChargingPilePlatform.git
cd ChargingPilePlatform

# ② 切到自己的分支（换成自己的 GitHub 账号名）
git checkout -b feat/你的GitHub账号 origin/feat/你的GitHub账号
```

| 成员 | GitHub | 分支 |
|---|---|---|
| 张芮萌 | littlez05 | feat/littlez05 |
| 葛伊诺 | geyinuo | feat/geyinuo |
| 毛悦琮 | maoyuecong682 | feat/maoyuecong682 |
| 陈庚泉 | flavourcatie | feat/flavourcatie |
| 吴羽桐 | shimmer-ywt | feat/shimmer-ywt |

## 2. 每日开发循环

```bash
# ① 先同步 main 的最新内容到自己分支（每天开工第一件事）
git checkout main
git pull origin main
git checkout feat/你的GitHub账号
git merge main              # 有冲突就当场解决，或喊组长/相关人一起

# ② 在自己的模块目录里写代码
#    （例：葛伊诺改 src/userclient/，毛悦琮/陈庚泉改 src/pcserver/，
#      吴羽桐改 src/webdashboard/ 与 common 中的通信组件）

# ③ 提交（小步提交，做完一个功能点就提交一次）
git add -A
git commit -m "[模块名] 简述做了什么"

# ④ 推送自己的分支
git push origin feat/你的GitHub账号
```

提交信息示例：`[用户端-查站] 完成附近电站列表与距离排序`、`[数据库] schema 增加 pile_health_metrics 表`。

## 3. 合入 main 的流程（Pull Request）

1. 一个**模块完成并自测**后，在 GitHub 网页上发起 Pull Request：`feat/你的账号` → `main`；
2. PR 描述里写明：完成的需求 NO.、功能点、自测结果（最好附截图）；
3. 组长（张芮萌）或指定同学 review 通过后合并；合并后你的分支执行一次 `git merge main` 同步。

> 可选：不会用 PR 的同学，可以把自己分支最新提交告诉组长，由组长在本地确认后合入——但必须保证 main 可编译。

## 4. 各成员细化流程

### 张芮萌（组长，feat/littlez05）
- 负责：NO.1-3 项目管理、NO.20-23（错误处理/测试打包/两个创新点）。
- 关键动作：
  1. 维护需求矩阵、分工与进度记录，每次周会更新状态；
  2. review 各成员 PR，保证 main 可编译可运行；
  3. 创新点：在服务器端实现价格策略引擎与自愈检查定时任务（基于吴羽桐预留的预测/阈值接口 + 陈庚泉的营销策略表/健康表）。
- 产出：集成测试记录、评审材料、最终打包。

### 葛伊诺（feat/geyinuo）
- 负责：NO.4-7 充电用户端（查站/导航/用户/充电），目录 `src/userclient/`。
- 关键动作：
  1. 先等陈庚泉的 schema 与 common 合入 main，再开发数据相关功能；
  2. 导航功能需申请腾讯地图 key（联网演示），代码里做好“无网降级”；
  3. 每完成一个▲模块自测一次并提交。
- 依赖：stations/piles/orders/users 表；Socket 上报接口（吴羽桐提供组件）。

### 毛悦琮（feat/maoyuecong682）
- 负责：NO.8-11（管理员登录/销售业绩/电桩状态/充电桩管理），目录 `src/pcserver/`。
- 关键动作：
  1. 销售业绩用 QChart（骨架已带 charts 模块），先做“近7/30日营收折线 + 今日/本月/总营收”；
  2. 电桩管理含“远程重启”（模拟指令，创新点2 会把该动作自动化）；
  3. 完成后发起 PR，命名如 `feat(server-sales)`。
- 依赖：orders/piles 表聚合查询（可参考陈庚泉的 sql 风格）；建议先自己写 SQL 小样再集成。

### 陈庚泉（feat/flavourcatie）
- 负责：NO.12-15（充电站管理/用户管理 + 数据库端全部），目录 `src/pcserver/` + `src/database/` + `src/common/`。
- 关键动作：
  1. **第一优先级**：把 schema.sql 最终版与 common.h 通信协议定义整理好，PR 合入 main——这是全组开工的地基；
  2. 提供“各表常用查询 SQL/接口约定”给毛悦琮、葛伊诺、吴羽桐；
  3. 完成充电站管理、用户管理（冻结/解冻、模糊搜索）。
- 注意：你的分支上次的 sales-dashboard 提交已按组里决定删除，请以 main 最新版为基准重新 checkout 自己的分支（本地备份标签 backup/sales-dashboard-43b3d14 可作参考，如组长同意可恢复）。

### 吴羽桐（feat/shimmer-ywt）
- 负责：NO.16-19（Web 大屏/机器学习简化预测/界面美化/Socket 多线程），目录 `src/webdashboard/` + `src/common/`（Socket 组件）+ 全局 QSS。
- 关键动作：
  1. **先接受 GitHub 协作者邀请**（否则推不了代码）；
  2. 联网下载一次 `echarts.min.js` 放入 `src/webdashboard/vendor/`（离线可跑）；
  3. 提供 Socket 通信组件与 ML 简化预测接口（输出写 marketing_strategy / pile_health_metrics 表，供张芮萌创新点使用）。
- 依赖：陈庚泉的表结构与 SQL 约定。

## 5. 集成顺序（建议里程碑）

| 里程碑 | 内容 | 涉及 |
|---|---|---|
| M0（已完成） | 仓库 + 需求矩阵 + 骨架 | 全组 |
| M1 | schema.sql、common 通信协议合入 main | 陈庚泉 |
| M2 | 各端独立功能开发（用户端/服务器端/大屏） | 葛/毛/陈/吴 |
| M3 | Socket 联调 + 界面美化统一 | 吴牵头，全组接入 |
| M4 | 两个创新点（价格策略/自愈）+ 集成测试 | 张牵头 |
| M5 | 评审材料 + 打包演示 | 张 + 全组 |

## 6. 常见问题

- **push 被拒**：先 `git pull`（或 `git merge origin/main`）解决分叉，再 push；不要用 `--force`。
- **误提交了构建产物**：加入 .gitignore 或 `git rm --cached` 后提交修正，不要删除别人代码。
- **网络慢**：SSH 已配置走 443 端口（~/.ssh/config），如还慢可改用 HTTPS 或让网络好的同学代推。
