# 项目交接文档（重构版）

当前代码从 `feat/littlez05-*` 分支按模块提交，由组长授权的littleZ05账号自主审核PR。PR30–34已合并：验证工具、业务闭环、导航、预测线程/推荐、自愈与仓储拆分。交付分支还在完成最终验收；以Git提交及最终测试报告确认版本，不使用旧“132项全部通过”口径。

## 入口

- `docs/使用手册.md`：环境、十个后台页签、充电、资料、导航、演示库及恢复。
- `docs/测试方案.md`：验证命令、关键异常、真实进程UI与数据保护测试。
- `docs/需求验收追踪.md`：23项需求的证据和未验收边界。
- `docs/协议与数据库变更评审_v3.md`：协议兼容性、身份、回执和迁移。
- `docs/重构结构说明.md`：拆分后的职责与文件。

```bash
./tools/demo_5min.sh
python3 tools/verify.py
python3 tools/package_delivery.py
```

## 已修复的核心问题

订单按用户身份和服务器ID操作；单用户/单桩活动订单唯一；开单锁价；界面在服务器确认后显示完成。余额不足、丢回包和重登恢复已通过真实服务进程与Qt界面测试。头像/昵称持久化；客户端配置使用明确组织名，待确认请求可保存和重放。

后台注入同一个StationStore连接，管理操作使用事务，活动订单不能被改桩/重启破坏，历史账务不能随删桩清掉。主窗口和仓储已按职责拆分，后台手写源文件均控制在1000行以内。

自愈按新样本推进，游标/状态/事件同时提交；不会重复消费历史样本升级故障，也不会释放活动订单。阈值校准需正常样本且不允许在故障中重学基线。

预测支持1/6/24小时，后台线程按值计算；按桩小时均值聚合并标记缺测插补。计价/推荐要求12小时至少8小时有记录，演示曲线不参与计价。

## 数据与运行

SQLite现有11张表：原9表加request_receipts、selfheal_cursors。金额REAL元，最终扣款舍入到分。v3升级整体事务执行，冲突活动订单导致回滚，不自动删单。升级前停服务并备份。

独立演示库为build-demo/chargingpile.db，普通演示保留数据，--reset先备份再替换。程序默认库仍在可执行文件旁，可用PCSERVER_DB_PATH指定。默认端口9999/8890；账号admin/123456，用户11位手机号免密注册。

## 边界与后续

腾讯地图在线验收尚缺Key；WebEngine本地加载与路线参数测试已通过。功率、电量、支付、重启仍为课程模拟。预测是简化站级OLS/WMA，未实现天气/节假日多维训练和桩级ML。主动优惠推送、物理桩端及生产认证未实现，勿当作完成项演示。

旧的独立演示目录、外部提交包和历史PDF/DOCX不自动更新；当前主项目使用仓库源码和最新Markdown说明。不要运行旧脚本或手工SQL清库替代迁移。
