# 交接文档 · 东软电动汽车充电桩应用管理平台（第 4 组）

> 最后更新：2026-09-15
> 交接时仓库状态：`main` @ `731ed6e`（工作区干净）
> 适用对象：接手本项目的组员 / 指导老师 / 后续维护者

---

## 0. 三十秒速览

这是一个"五端联动"的充电桩运营平台：**用户端（Qt）→ 服务器端（Qt）→ 数据库（SQLite）→
机器学习（负荷预测）→ Web 大屏（ECharts）**。用户端充一次电，订单会同时出现在服务器后台
销售业绩、数据库订单表和大屏今日营收里，三处数字一致。

**拿到代码后，跑这一条命令就能看到完整演示：**

```bash
cd /home/bit/桌面/dongruan_ws/ChargingPilePlatform
./tools/demo_5min.sh
```

---

## 1. 代码与仓库

| 项目 | 值 |
|---|---|
| GitHub | `git@github.com:littleZ05/ChargingPilePlatform.git` |
| 主分支 | `main`（始终保持可编译可运行） |
| 交接提交 | `731ed6e feat(自检): SelfCheckPanel 支持关闭服务器专属服务检查` |
| 开发机路径 | `/home/bit/桌面/dongruan_ws/ChargingPilePlatform`（虚拟机 bitdev） |
| 当前分支 | `main`，与 `origin/main` 完全一致，工作区无未提交改动 |

### 分支约定（沿用）

- 每个成员按模块开 `feat/<模块名>` 分支，完成后在 GitHub 发起 Pull Request 合入 `main`；
- 提交信息格式：`[模块名] 简述做了什么`；
- **公共契约不可各改各的**：`src/database/schema.sql`、`src/common/common.h` 改动必须先评审；
- 构建产物（`*.o`、`moc_*`、`Makefile`、可执行文件）已被 `.gitignore` 排除，不要提交。

### 网络注意事项（本机特有）

- 本网络 **HTTPS 到 github.com 不通**，GitHub 走 SSH，且 22 端口不可用，
  已在 `~/.ssh/config` 配置 `ssh.github.com:443`；
- 终端里有代理注入（`LD_PRELOAD`），会让 `git pull` 失败，**统一用**：

  ```bash
  env -u LD_PRELOAD git pull --ff-only
  ```

  仓库里的演示脚本已内置这一处理。

---

## 2. 运行环境

| 项 | 值 |
|---|---|
| 操作系统 | Ubuntu 22.04.3 LTS（VMware 虚拟机） |
| Qt 版本 | Qt 6.2.4（`qmake6`、Widgets / Network / Sql / Charts） |
| 编译工具 | g++、make |
| 数据库 | SQLite（通过 Qt QSQLITE 驱动） |
| Web 大屏 | ECharts 5.6.1（本地 `vendor/`，完全离线） |
| 机器访问 | `ssh bitdev`（Windows 侧已配密钥免密；等价于 `ssh bit@192.168.3.131`） |

依赖安装（换机时）：

```bash
sudo apt install qt6-base-dev qt6-charts-dev libqt6sql6-sqlite g++ make
```

---

## 3. 快速开始

### 3.1 完整五端演示（推荐）

```bash
cd /home/bit/桌面/dongruan_ws/ChargingPilePlatform
./tools/demo_5min.sh              # 逐步推进，每步按回车（便于控场）
./tools/demo_5min.sh --auto       # 不暂停，直接全部拉起
./tools/demo_5min.sh --no-map     # 未配置腾讯地图 Key 时
```

脚本会自动：`git pull` 最新 main → 清理上次残留进程（避免 9999/8890 被占）→
重置演示库 → 启动 PcServer（登录框 admin/123456）→ 启动 UserClient →
打开 Web 大屏 → 打开数据佐证终端。

> ⚠️ 必须在**虚拟机桌面上的终端**里运行。纯 SSH 终端没有图形会话，
> Qt 程序会直接 abort（脚本会检测并提示 `export DISPLAY=:0`）。

### 3.2 分别构建

```bash
# 服务器端
cd src/pcserver && qmake6 && make -j4 && ./PcServer

# 用户端
cd src/userclient && qmake6 && make -j4 && ./UserClient

# Web 大屏（先起 PcServer，再双击打开）
xdg-open src/webdashboard/index.html
```

### 3.3 一键重置演示数据

```bash
./tools/seed_demo_data.sh
# 恢复为 5 站 / 37 桩 / 128 订单的干净演示库
```

### 3.4 账号

| 端 | 账号 |
|---|---|
| PcServer 管理员 | `admin` / `123456` |
| UserClient 用户 | 任意 11 位手机号（未注册自动注册，赠送初始余额 100 元） |
| 演示用手机号 | `13800138001`（已有余额记录） |

---

## 4. 五端架构与数据流

```
        ┌─────────────────────── TCP :9999 ───────────────────────┐
        │  帧格式："CP" + MsgType(2B, 大端) + BodyLen(4B, 大端) + JSON │
        ▼                                                          │
┌──────────────┐  MsgType 0/1/2/4/10/30   ┌──────────────────────┴───────┐
│  用户端       │ ────────────────────────▶ │  PC 服务器端                  │
│ userclient   │ ◀── 回包（含折后价、余额） │  NetServer(9999)             │
└──────────────┘                           │  DashboardApiServer(8890)    │
                                           │  PricingService（每 10s）     │
                                           │  SelfHealService（每 10s）    │
                                           │  StationStore（唯一数据访问层）│
                                           └───────┬──────────────────┬──┘
                                    forecastLoad() │                  │SQL
                                                   ▼                  ▼
                            ┌──────────────────────────┐   ┌──────────────────┐
                            │ 机器学习端                │◀──│  数据库端         │
                            │ cp::forecastLoad OLS/WMA │读取│ 9 张表（SQLite） │
                            └──────────────────────────┘   └──────────────────┘
                                                   ▲                  ▲
                                       HTTP :8890 │                  │
                                           ┌───────┴──────────────────┴──┐
                                           │  Web 大屏（10s 轮询 / 离线兜底）│
                                           └──────────────────────────────┘
```

**三条关键闭环**

1. **充电结算**：用户端建单 → 服务器事务内写 `orders(state=0)` 并置桩「充电中」→
   结束充电上报 → 服务器按「基础价 × 生效折扣 × 电量」计算金额、完成订单、扣余额、释放电桩 →
   后台销售业绩与大屏营收同步变化。
2. **创新点① 闲时动态计费**：负荷预测算出未来 1 小时空闲率 → 空闲率 > 60% 自动 8 折 →
   写入 `marketing_strategy`（含预测值与决策时间）→ 用户端查询时带出折后价与「闲时特惠」徽标 →
   **该折后价真实参与结算**。
3. **创新点② 异常自愈**：设备上报功率样本 → 连续 3 次低于阈值 → 标记「预警（需检查）」并自动
   远程重启 → 仍异常升级「故障」，样本恢复则解除 → 全过程写入 `selfheal_events`。

---

## 5. 交接前完成的关键改造（重要）

接手人需要知道：**2026-09-11 之前，这套系统存在"仿真与真实混用"的问题，现已修复。**

### 5.1 修复了"双库分裂"（最关键）

改造前，管理后台（登录 / 销售业绩 / 电桩状态 / 充电桩管理 / 用户管理）读的是
`~/.local/share/ChargingPilePlatform/PcServer/chargingpile-platform.db`，
而 Socket 服务、大屏、预测、定价、自愈读的是 `<程序目录>/chargingpile.db`。
两个库数据完全不同，**导致用户端充完电，管理员在业绩里永远看不到**。

现由 `main.cpp` 注入统一路径（`pcserver::setAdminDatabasePath`），全进程只有一个库。
**后续任何新增模块都必须通过 `StationStore` 访问数据库，不要再建第二个连接层。**

### 5.2 修复了用户端桩号与数据库不一致

用户端原本本地合成桩号 `S1-01`，数据库里是 `S01-P01`，导致"结束充电"必然返回
404「电桩编码不存在」。现已对齐，且站内电桩列表改为**服务器返回的真实数据**
（`kStationQuery` 带 `station_id` 时返回 `piles[]`）。

### 5.3 把"本地假动作"改成真实链路

| 原实现 | 现实现 |
|---|---|
| 余额充值 `m_balance += amount`（纯内存） | `kRechargeRequest(30)` 真实写入 `users.balance` |
| 结算金额由用户端计算 | 服务器按执行价重算，客户端上报仅作对账 |
| 开始充电只是本地计时 | `kStartCharge(4)` 服务器事务建单 + 置桩充电中 |
| 自愈"恢复"靠伪造一条功率日志 | 只用设备上报的真实样本，动作写入事件表 |
| 随机翻转电桩状态模拟"实时" | 默认关闭，需 `PCSERVER_SIMULATE=1` 显式开启 |
| 特惠判定可被前端本地空闲率触发 | 服务器数据优先，保证展示价 = 结算价 |

### 5.4 新增的服务器端页签（共 10 个）

销售业绩、负荷预测、电桩状态、充电站管理、用户管理、充电桩管理、
**价格策略（NO.22）**、**自愈告警（NO.23）**、**运行日志（NO.20）**、**交付自检（NO.21）**。

---

## 6. 数据库结构

位置：`<程序目录>/chargingpile.db`，可用环境变量 `PCSERVER_DB_PATH` 覆盖。
建表脚本是唯一契约：`src/database/schema.sql`（编译进 qrc 资源，启动时自动执行）。

**9 张表**（5 张核心 + 4 张支撑）：

| 表 | 说明 |
|---|---|
| `admins` | 管理员账号（演示密码明文，生产需改哈希） |
| `users` | 用户（手机号唯一、余额、状态 0 正常/1 冻结） |
| `stations` | 充电站（经纬度、总桩数、在线率、基础电价） |
| `piles` | 电桩（`code` 全局唯一、类型、功率、`state` 0 闲置/1 充电中/2 故障） |
| `orders` | 订单（`state` 0 充电中/1 已完成/2 取消） |
| `marketing_strategy` | 营销策略（创新点①：折扣、`predicted_idle_rate`、`decided_at`） |
| `pile_health_metrics` | 电桩健康阈值（创新点②） |
| `pile_power_logs` | 每次充电的实功率记录（创新点② 判定数据源） |
| `selfheal_events` | 自愈事件留痕（创新点② 前端"自愈告警"页数据源） |

**本次新增的列**（已在 `schema.sql` 中，旧库启动时自动 `ALTER TABLE` 补齐）：

- `piles.health_level`：0 正常 / 1 预警（需检查）/ 2 故障 —— 支撑电桩三级分级；
- `marketing_strategy.predicted_idle_rate`、`decided_at` —— 让定价依据可追溯。

**其它约定**：两个一致性触发器保证 `orders.pile_id` 与 `station_id` 匹配；
8 个业务索引；全部 SQL 参数化绑定。

> ⚠️ 已知口径不一致：`概要设计说明书` 与答辩讲稿写"金额按分存储"，
> 但代码实际是 `REAL`（元）。**以代码为准**，后续若统一需同时改文档。

---

## 7. 通信协议（Socket 9999）

完整文档见 `docs/socket-protocol.md`（含 v1.1 补充章节）。实际端到端使用的消息类型：

| 值 | 常量 | 方向 | 用途 |
|---|---|---|---|
| 0 | `kHeartbeat` | 双向 | 心跳 / ACK |
| 1 | `kStationQuery` | 双向 | 电站列表；带 `station_id` 时额外返回 `piles[]` 真实桩列表 |
| 2 | `kOrderReport` | 双向 | 充电结算上报（金额由服务器重算） |
| 4 | `kStartCharge` | 客户端→服务器 | 开始充电建单 |
| 10 | `kLoginRequest` | 客户端→服务器 | 手机号登录 / 自动注册 |
| 30 | `kRechargeRequest` | 客户端→服务器 | 余额充值 |

应答使用与请求相同的 MsgType；错误码：400 请求非法 / 403 权限受限（冻结账号）/
404 资源不存在 / 409 状态冲突 / 422 参数校验失败 / 503 服务未就绪。

> `common.h` 中还声明了 `kPileStateReport(3)`、`kStopCharge(5)`、`kProfileQuery(6)` 等
> 预留常量，**目前未实现**，接手时不要误以为已有功能。

大屏数据接口：`GET http://127.0.0.1:8890/api/dashboard/overview`（只读聚合，带 CORS 头），
存活探测用 `/api/dashboard/health`。

---

## 8. 目录结构

```
ChargingPilePlatform/
├── README.md                      # 项目简介与团队约定
├── 交接文档.md                     # 本文件
├── 分工与进度记录.md                # 成员分工
├── 开发流程.md                     # 每日同步 / PR / 合并流程
├── docs/                          # 全部文档（需求矩阵、概要设计、答辩材料、协议…）
├── tools/
│   ├── demo_5min.sh               # ★ 五端联动演示脚本（5 分钟版）
│   ├── seed_demo_data.sh          # ★ 一键重置演示数据
│   ├── final_demo.sh              # 早期整体演示脚本（仍可用）
│   ├── midterm_demo.sh            # 中期评审脚本
│   └── new_features_demo.sh       # 新增功能分步演示
└── src/
    ├── ChargingPilePlatform.pro   # 总工程（subdirs）
    ├── common/                    # 公共契约与公共算法
    │   ├── common.h               # 端口 / 状态枚举 / Pricing / SelfHeal 常量
    │   ├── protocol.h             # 版本化安全 Envelope（NO.15）
    │   ├── error_utils.h          # NO.20 错误码 / 文案 / 输入校验
    │   ├── ui/error_notify.h      # NO.20 统一弹窗与状态栏提示
    │   ├── loadforecast.*         # NO.17 负荷预测（OLS / WMA）
    │   ├── net_server.* net_client.* packet_assembler.*   # Socket 组件
    │   └── tests/                 # 5 个公共组件测试工程
    ├── userclient/                # 充电用户端（葛伊诺）
    ├── pcserver/                  # PC 服务器端（毛悦琮 / 陈庚泉 / 张芮萌）
    │   ├── stationstore.*         # ★ 唯一数据访问层
    │   ├── pricingservice.*       # NO.22 价格策略引擎
    │   ├── selfhealservice.*      # NO.23 自愈检查服务
    │   ├── opsconsole.*           # NO.20/21/22/23 四个前端页签
    │   └── tests/                 # 8 个服务器端测试工程
    ├── database/                  # schema.sql（契约）+ demo_seed.sql
    ├── webdashboard/              # ECharts 大屏（index.html + 本地 vendor）
    └── innovation_demo/           # 早期独立创新点演示器（与主链路无关联）
```

---

## 9. 测试、演示与答辩材料

| 材料 | 位置 | 说明 |
|---|---|---|
| 演示脚本 | `tools/demo_5min.sh` | 依次弹出演示所需窗口 |
| 口播稿 | `docs/演示口播稿.md` | 正文 879 字，约 4 分 30 秒，含时间轴与追问应答 |
| 结题答辩讲稿 | `docs/答辩讲稿_结题答辩_20260910.md` | 按人分段 |
| 答辩 PPT 文字稿 | `docs/答辩PPT_15页完整文字与排版_20260910.md` | 15 页逐页文字与排版 |
| 测试用例 | 提交版：`D:\vmware_share\...\03.项目文件\03测试用例.xls` | 14 个工作表 / 90 条用例，全部通过 |
| 需求矩阵 | `docs/01需求矩阵第4组张芮萌.xlsx` | 23 项需求 |
| 自动化测试 | `src/*/tests/` | 15 个测试工程，132 项用例 |

运行自动化测试（每个测试工程独立构建，参照仓库 `src/README.md`）：

```bash
# 公共组件（协议 / 错误处理 / 组包器 / Socket）
cd src/common/tests
qmake6 protocol_tests.pro        && make -j4 && ./tst_protocol
qmake6 error_utils_tests.pro     && make -j4 && ./error_utils_tests
qmake6 packet_assembler_tests.pro && make -j4 && ./tst_packet_assembler

# 服务器端（数据库契约 / 数据访问 / 服务 / 大屏接口 / Socket 业务）
cd src/pcserver/tests
qmake6 schema_tests.pro          && make -j4 && QT_QPA_PLATFORM=offscreen ./tst_schema
qmake6 stationstore_tests.pro    && make -j4 && QT_QPA_PLATFORM=offscreen ./tst_stationstore
qmake6 tst_socketbiz.pro         && make -j4 && QT_QPA_PLATFORM=offscreen ./tst_socketbiz

# 用户端
cd src/userclient/tests
qmake6 pcserver_session_tests.pro && make -j4 && QT_QPA_PLATFORM=offscreen ./tst_pcserver_session
```

带界面的测试工程（如 `stationui_tests.pro`）需要图形会话，无桌面时加 `QT_QPA_PLATFORM=offscreen`。

---

## 10. 已知限制与后续待办 ⚠️

> 这一节是交接重点：以下都是**目前真实存在**的限制，不要当成已完成功能去讲。

### 10.1 功能层面

1. **远程重启没有真的下发到设备端**。
   现在 `MainWindow::sendRemoteRestart()` / 自愈服务只是复位数据库中的电桩运行状态，
   没有通过 Socket 向设备发送指令——因为项目里**没有独立的"桩端"进程**。
   要做真，需要新增一个桩端模拟器进程，服务器通过 `NetServer` 向它下发指令、由它回报状态。
2. **自愈的"恢复"路径需要一根正常功率样本**才会解除预警。
   演示库中 S03-P02 的样本一直是低的，所以会走到"升级故障"。
   演示完整闭环请在「自愈告警」页点「注入正常功率样本」。
3. **改昵称仍是本地改动**，未接 `kProfileUpdate`；头像上传未实现。
4. **负荷预测结果不单独落库**，页面与 8890 接口是按需实时计算；
   落库的只有定价依据 `marketing_strategy.predicted_idle_rate`。
5. **充电过程中的电量是本地按"功率 × 时间"累加的展示值**，
   最终结算金额以服务器为准（服务器用上报的 kWh × 执行价）。
6. **服务器不可达时**，用户端会退回腾讯 POI 列表，此时卡片上的价格/空闲桩数是
   本地稳定哈希生成的**模拟值**（代码中已注释说明）。
7. **管理员密码明文存储**（演示用），生产环境需改为加盐哈希。
8. `innovation_demo/` 是早期独立演示器，与主链路无关联，可按需删除。
9. `common.h` 中 15 个 MsgType 常量只有 6 个真正实现（见 §7）。

### 10.2 待清理项

- 旧库残留：`~/.local/share/ChargingPilePlatform/PcServer/chargingpile-platform.db`
  是修复双库问题前的产物，确认无用后可删除；
- `docs/` 中"金额按分存储"的表述与代码不符（见 §6 注释）。

### 10.3 建议的后续优先级

| 优先级 | 事项 | 预估 |
|---|---|---|
| P1 | 新增桩端模拟器进程，让远程重启/状态上报真正走协议 | 2 人天 |
| P2 | 补齐 `kStopCharge` / `kProfileQuery` / `kProfileUpdate`，让充电与资料全链路走协议 | 2 人天 |
| P3 | 统一金额单位口径（改文档），管理员密码改哈希 | 0.5 人天 |
| P4 | 服务器端会话注册表 + 主动推送（当前"特惠推送"靠用户端查询带回） | 1 人天 |

---

## 11. 故障排查

| 现象 | 原因 / 处理 |
|---|---|
| 双击 `PcServer` 无反应，日志报 `could not connect to display` | 在 SSH 终端里运行了 Qt 程序。请在桌面终端运行，或 `export DISPLAY=:0` |
| 端口 9999 / 8890 被占用 | `for p in $(pgrep -f PcServer); do kill $p; done`，或直接重跑 `tools/demo_5min.sh`（脚本会自动清理） |
| `git pull` 卡住或失败 | 终端代理干扰，改用 `env -u LD_PRELOAD git pull --ff-only` |
| `git push` 报 `Connection reset ... port 443` | HTTPS 不通，属正常。直接重试（走 `ssh.github.com:443`）；仍失败检查 `~/.ssh/config` |
| 大屏显示 `--` 或空白 | PcServer 没起或 8890 没监听。用 `curl http://127.0.0.1:8890/api/dashboard/health` 验证 |
| 用户端提示"结算失败 404 电桩编码不存在" | 桩号与数据库不一致（老问题，已修）。确认代码在 `main` 最新提交，且不要手工改 `stationpage.cpp` 的前缀格式 |
| 后台销售业绩不随充电变化 | 双库问题复发。检查是否有人重新引入了第二个数据库连接层 |
| 自愈页没有事件 | 需先有 ≥3 条低功率样本。可在页面注入，或确认 `SelfHealService` 已启动 |
| `/home/bit/vmware_share` 报"传输端点尚未连接" | VMware 共享目录挂载失效。执行 `sudo umount /home/bit/vmware_share && sudo mount -t vmhgfs-fuse .host:/ /home/bit/vmware_share -o allow_shared_folders,allow_other`，或 `sudo systemctl restart open-vm-tools` |
| 登录框没弹出来 | 环境变量 `PCSERVER_AUTOLOGIN=1` 会跳过登录（自动化演示用），清掉即可 |

---

## 12. 交付物位置

### 12.1 仓库内

- 全部源码、文档、脚本、`schema.sql`；
- 演示脚本 `tools/demo_5min.sh`、口播稿 `docs/演示口播稿.md`。

### 12.2 仓库外（本机）

| 交付物 | 位置 |
|---|---|
| 全部代码压缩包 | `D:\vmware_share\ChargingPilePlatform_全部代码_20260911.zip`（7.5 MB，含 `.git` 历史） |
| 组长模块独立演示包 | `D:\vmware_share\组长模块_独立演示_张芮萌_20260911.zip`（199 KB） |
| 独立包可运行目录 | 虚拟机 `/home/bit/桌面/dongruan_ws/组长模块_独立演示_v2/`（`./build_and_run.sh` 一键编译运行） |
| 已填写的测试用例 | `D:\vmware_share\...\03.项目文件\03测试用例.xls`（空白模板备份为同目录 `03测试用例_空白模板备份.xls`） |
| 演示库备份 | 虚拟机 `src/pcserver/chargingpile.db`（可用 `tools/seed_demo_data.sh` 重置） |

### 12.3 组长模块独立包说明

`组长模块_独立演示_v2/` 是把**张芮萌负责的四个需求**（NO.20 错误处理 / NO.21 测试与交付 /
NO.22 闲时动态计费 / NO.23 异常自愈告警）从主项目中抽出、去掉 Socket 与大屏依赖后的
**可独立编译运行**的工程：

```bash
cd /home/bit/桌面/dongruan_ws/组长模块_独立演示_v2
./build_and_run.sh              # 编译 + 运行
./build_and_run.sh --reset      # 重置演示库后运行
./build_and_run.sh --offscreen  # 无显示器环境自检
```

共 4 个页签：价格策略 / 自愈告警 / 运行日志 / 交付自检。
其中"交付自检"的两项服务检查（Socket 9999、大屏 8890）在独立包中标为 `N/A`。

---

## 13. 联系与责任划分（参考 `分工与进度记录.md`）

| 成员 | 负责范围 |
|---|---|
| 张芮萌（组长） | 项目管理、公共契约、错误处理（NO.20）、测试与交付（NO.21）、创新点① ②（NO.22/23）、演示与答辩材料 |
| 葛伊诺 | 充电用户端（NO.4 ~ NO.7） |
| 毛悦琮 | 服务器端基础管理、销售业绩、电桩状态、充电桩管理（NO.8 ~ NO.11） |
| 陈庚泉 | 数据库端（NO.14/15）、充电站管理、用户管理、通信数据结构（NO.12/13） |
| 吴羽桐 | Web 大屏（NO.16）、机器学习负荷预测（NO.17）、Socket 通信组件（NO.19） |

---

> **交接完成标志**：接手人能在虚拟机上执行 `./tools/demo_5min.sh`，
> 看到用户端充一次电后，后台销售业绩与大屏营收同步变化，即为环境与代码均正常。
